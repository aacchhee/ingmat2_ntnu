name: Re-ingest notes on push

on:
  push:
    branches: ["main"]

jobs:
  reingest:
    runs-on: self-hosted
    steps:
      - name: Debug user + sudo
        run: |
          whoami
          id
          sudo -n -l || true
