name: Re-ingest notes on push

on:
  push:
    branches: ["main"]

jobs:
  reingest:
    runs-on: self-hosted
    steps:
      - name: Re-ingest
        run: |
          sudo -n /usr/local/bin/rag_update_and_ingest
        
